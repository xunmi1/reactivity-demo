{
  "name": "reactivity",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc && vite build",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^16.11.26",
    "c8": "^7.11.0",
    "typescript": "^4.6.3",
    "vite": "^2.9.1",
    "vitest": "^0.9.0"
  }
}
